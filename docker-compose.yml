services:
  jekyll:
    image: jekyll/jekyll:latest
    command: jekyll serve --watch --livereload --incremental
    ports:
      - "4000:4000"
    volumes:
      - .:/srv/jekyll
      - bundle:/usr/local/bundle

volumes:
  bundle:
